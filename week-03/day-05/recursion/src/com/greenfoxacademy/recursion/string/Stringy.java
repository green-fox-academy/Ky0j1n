package com.greenfoxacademy.recursion.string;
// Given a string, compute recursively (no loops) a new string where
// all the lowercase 'x' chars have been changed to 'y' chars.

public class Stringy {
    public static Stringy lowercaseXtolowercaseY(String incomingString){
        

         if(incomingString.length() != 0){
             if(incomingString.charAt(incomingString.length() - 1);

         }
    }

}
